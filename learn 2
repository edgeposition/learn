#define _CRT_SECURE_NO_WARNINGS 1

#include <stdio.h>

enum Sex
{
    MALE,
    FEMALE = 3,//赋初值 从被赋初值的数后,每个数都会跟着改变
    SECRET
    //枚举变量可能的取值
    //枚举常量 本质是数字 从零开始 0,1,2.....
};

int main(void)
{
    printf("%d %d %d", MALE, FEMALE, SECRET);
   
    return 0;
}
