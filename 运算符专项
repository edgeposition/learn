#define _CRT_SECURE_NO_WARNINGS 1

#include <stdio.h>

int main(void)
{
    int i = 4;
    int j = 1;
    int m = 1;

    m=i/j
    (m%2==0)

    int y=1
        (y<10 && y>-10) || (y>100 || y<-100)

    int x=1
    (x%4==0 && x%100!==0) || (x%400==0)


    printf("%d", m);

    return 0;
}
